<Page x:Class="VkPoster.View.ProgressView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:VkPoster.View"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="700"
      Title="WorkPage"
      DataContext="{Binding Groups, Source={StaticResource Locator}}">

    <Page.Resources>

    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="190*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="59*" />
            <RowDefinition Height="9*"/>
            <RowDefinition Height="55*"/>
            <RowDefinition Height="225*"/>
            <RowDefinition Height="352*"/>
        </Grid.RowDefinitions>

        <Label Content="Working..."
               Grid.Row = "0" Margin="0,0,0,10.2"
               Background="LightBlue"
               FontSize="20"
               FontStretch="Expanded"
               FontWeight="ExtraBold"/>
        <TextBlock Margin="10,7.8,10,9" Grid.Row="3" TextWrapping="Wrap" Text="Dont close application, bot will work and post news"
                   FontSize="20"
                   />
    </Grid>
</Page>
